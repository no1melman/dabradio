namespace DabRadio.StateMachine
{
    public enum RadioStateTransistion
    {
        GetRadioDevice,
        Stop,
        Play,
        GetStations,
        SelectDevice,
        TurnOnDevice,
        TurnOffDevice
    }
}